using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PauseScreenManager : MonoBehaviour
{
    /*
    GameObject pauseOverlay;

    private void Start()
    {
        if (gameObject.transform.childCount > 0)     //Checks if obj has children (otherwise will throw error when running "GetChild")
        {
            //WILL BREAK IF 'TestPauseOverlay' IS MOVED IN HIERARCHY, need to change the number here if moved!!!
            pauseOverlay = gameObject.transform.GetChild(4).gameObject;      //Sets var to store the 'TestPauseOverlay' obj in the Canvas
                                                                             //Pause screen is GetChild(3), currently set to Tutorial(4)
        }
    }

    void Update()
    {
        
        if (ControlManager.paused)
        {
            pauseOverlay.SetActive(true);
        }
        else
        {
            pauseOverlay.SetActive(false);
        }
        
    }
    */
}
