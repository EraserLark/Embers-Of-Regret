using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemInteractTest : ObjectInteractable //  ObjectInteractable << Monobehavior
{
    /*
    public int itemNum;

    //what to setactive for inventory
    public GameObject Item;

    public override void Selected()
    {
        base.Selected();

        if(state && MouseInteract.playerItemNumber == -1)
        {
            //print(itemNum);     //DEBUG
            MouseInteract.playerItemNumber = itemNum;      //Sets var in Player to correspond to the item now being 'held'
            MouseInteract.playerHoldingItem = true;     //Player cannot pick up another item while this is true
            gameObject.SetActive(false);                //Makes the 'item' obj being clicked on disappear (now collected in your hand)
            Item.SetActive(true);                       //Set the corresponding 'ItemHeld____' to true, which will appear in the player's hand
        }
    }
    */
}
