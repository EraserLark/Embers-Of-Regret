using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TutorialManager : MonoBehaviour
{
    /*
    GameObject tutorialOverlay;
    bool isOpen = true;

    // Start is called before the first frame update
    void Start()
    {
        if (gameObject.transform.childCount > 0)     //Checks if obj has children (otherwise will throw error when running "GetChild")
        {
            //WILL BREAK IF 'TestPauseOverlay' IS MOVED IN HIERARCHY, need to change the number here if moved!!!
            tutorialOverlay = gameObject.transform.GetChild(4).gameObject;      //Sets var to store the 'TestPauseOverlay' obj in the Canvas
        }
    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Tab))
        {
            if(isOpen)
            {
                tutorialOverlay.SetActive(false);
                isOpen = false;
            }
            else if (isOpen == false)
            {
                tutorialOverlay.SetActive(true);
                isOpen = true;
            }
        }
    }
    */
}
